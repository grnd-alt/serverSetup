server {
    listen 80;
    listen [::]:80;
    server_name _;
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
	index test.html;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name opfer.${SERVER_BASE_DOMAIN} www.opfer.${SERVER_BASE_DOMAIN};
    server_tokens off;
    location / {
        root   /usr/share/nginx/html/opfer/;
        index  index.html index.htm;
    }
}


server {
    listen 80;
    listen [::]:80;
    server_name ${SERVER_BASE_DOMAIN} www.${SERVER_BASE_DOMAIN};
    server_tokens off;
    location / {
        root   /usr/share/nginx/html/home/;
        index  index.html index.htm;
    }
}

server {
    listen       80 default_server http2;
    listen [::]:80 http2;
    server_name  nextcloud.${SERVER_BASE_DOMAIN};
    server_tokens off;
    add_header Strict-Transport-Security "max-age=15768000; includeSubDomains; preload;";
    client_max_body_size 4196M;

    # #access_log  /var/log/nginx/host.access.log  main;

    # location / {
    #     root   /usr/share/nginx/html;
    #     index  index.html index.htm;
    # }

    location / {
        proxy_pass http://localhost:11000$request_uri;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_set_header X-Forwarded-Scheme $scheme;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Accept-Encoding "";
        proxy_set_header Host $host;
    
        client_body_buffer_size 512k;
        proxy_read_timeout 86400s;
        client_max_body_size 0;

        # Websocket
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
    ssl_certificate /etc/letsencrypt/live/${SERVER_BASE_DOMAIN}/fullchain.pem;   # managed by certbot on host machine
    ssl_certificate_key /etc/letsencrypt/live/${SERVER_BASE_DOMAIN}/privkey.pem; # manag

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
